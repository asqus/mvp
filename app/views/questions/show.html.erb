<p id="notice"><%= notice %></p>

<div class="q_title">
  <%= @question.title %>
</div>

<div class="q_body">
  <%= @question.body %>
</div>
<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Back', questions_path %>

<p>
<div class="response_list">
      <% @question.responses.each do |response| %>
      <div class="response_block">
        <div class="response_body"><%= response.body %></div>
        <div class="response_user"><%= link_to response.user.name, response.user %></td>
      </div>
      <% end %>
</div>
<%= link_to 'New Response', new_response_path(:questionID => @question.id) %>
